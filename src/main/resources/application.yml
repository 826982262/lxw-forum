server:
  port: 8090
  tomcat:
    uri-encoding: UTF-8

spring:
  resources:
    static-locations: classpath:/
  mvc:
    static-path-pattern: classpath:/static
    view:
      suffix: .html
  thymeleaf:
    cache: false
    encoding: utf-8
    mode: HTML5
    prefix: classpath:/templates/
    suffix: .html
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/forum?useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&autoReconnect=true&useSSL=false&allowMultiQueries=true
    url: jdbc:mysql://119.29.79.150:3306/forum?useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&autoReconnect=true&useSSL=false&allowMultiQueries=true
    username: root
    password: 667480
    hikari:
      minimum-idle: 5
      maximum-pool-size: 15
      auto-commit: true
      idle-timeout: 30000
      pool-name: hikarCP
      max-lifetime: 30000
      connection-test-query: SELECT 1
  redis:
    host: 119.29.79.150
    password: 667480wohenni@
    lettuce:
      pool:
        max-active: 5
        max-idle: 1
        max-wait: 5000
    timeout: 30000
    port: 6379

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

mybatis:
  type-aliases-package: edu.gzhh.forum.mapper
  mapper-locations: classpath:mapper/*Mapper.xml
  ############# 分页插件PageHelper配置 #############

pagehelper:
  auto-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  page-size-zero: true
  params: count=countSql



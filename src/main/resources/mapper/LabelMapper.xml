<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.gzhh.forum.mapper.LabelMapper">
<select id="selectOrderByRank" resultType="edu.gzhh.forum.entity.Label">
    select l_id,l_name,ranking from label
    order by ranking desc
    limit #{start},#{limit}
</select>
</mapper>
